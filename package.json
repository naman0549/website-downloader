{
  "name": "website-downloader",
  "version": "1.0.0",
  "description": "Downloads a webpage plus its CSS, JS, images, fonts, videos and icons, rewrites links, and saves it as a working offline copy (optionally zipped). Supports rendering JS-heavy (React/Vue/etc) sites via --render.",
  "type": "module",
  "bin": {
    "website-downloader": "./cli.js"
  },
  "scripts": {
    "start": "node cli.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "archiver": "^7.0.1",
    "cli-progress": "^3.12.0",
    "playwright": "^1.47.0"
  }
}
